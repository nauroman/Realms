using UnityEngine;
using System.Collections;

namespace com.flashunity.realms.tiles
{
	public class Tiles : MonoBehaviour
	{

		// Use this for initialization
		void Start ()
		{
			/*
			int l = transform.childCount;

			for (int i=0; i<l; i++) {
				Transform child = transform.GetChild (i);

				Vector3 childLocalPosition = child.localPosition;

				child.localPosition = new Vector3 (childLocalPosition.x, childLocalPosition.y, -childLocalPosition.x / 1024 - childLocalPosition.y);
			}
			*/
		}
	
		// Update is called once per frame
		void Update ()
		{
	
		}
	}
}
